# DRAFT — tdesktop TGVerity v0.2 hook patch
# Generated: 2026-07-04 21:34 MSK
# Status: NOT test-applied. No tdesktop checkout existed at generation time
#   (clone running in background to /Users/dyakov/Claude/tdesktop).
#
# Every hunk below is BEST-EFFORT. Before `git apply` you MUST:
#   1. Re-pin all context lines + line numbers to the actual upstream commit
#      (the @@ line ranges and unchanged "context" lines here are illustrative).
#   2. Re-confirm symbol names against docs/message-lifecycle.md hook map
#      (e.g. registerSentMessageRandomIds vs legacy registerMessageRandomId;
#       skipNotification vs notifications_manager path).
#
# All additions are wrapped in
#     // TGVHOOK BEGIN (v0.2): <intent>
#     ...
#     // TGVHOOK END
# blocks and tagged with a `// TGVHOOK v0.2: <intent>` line so a human can
# finalize placement post-checkout.
#
# Hook points (5 needs, 5 files; api_updates.cpp carries 2 hunks):
#   send-wrap      api/api_sending.cpp
#   incoming       api/api_updates.cpp  (updateNewMessage)
#   id-bind        api/api_updates.cpp  (updateMessageID)
#   delete-revoke  data/data_histories.cpp (deleteMessages revoke=true)
#   notif suppress history/history_item.cpp  (skipNotification)
#   edit suppress  history/view/history_view_context_menu.cpp
#
# The shim itself (desktop/tgverity_bridge_shim.{h,cpp}) must be added to
# Telegram/CMakeLists.txt as a slim target/source set (see desktop/README.md).
# v0.1 branding patch (tdesktop-macos-tgverity-v0.1.patch) is RETAINED as-is.

diff --git a/Telegram/CMakeLists.txt b/Telegram/CMakeLists.txt
--- a/Telegram/CMakeLists.txt
+++ b/Telegram/CMakeLists.txt
@@ -0,0 +1,12 @@
+// TGVHOOK BEGIN (v0.2): build wiring — add shim sources to the Telegram target.
+// Re-pin: paste these into the target_sources(Telegram PRIVATE ...) block near
+// the other SourceFiles entries. Path assumes desktop/ shim is copied/symlinked
+// into the tdesktop tree at Telegram/SourceFiles/tgverity/.
+target_sources(Telegram
+PRIVATE
+    Telegram/SourceFiles/tgverity/tgverity_bridge_shim.h
+    Telegram/SourceFiles/tgverity/tgverity_bridge_shim.cpp
+)
+// TGVHOOK v0.2: build wiring — compile the standalone shim into Telegram.
+// TGVHOOK END
diff --git a/Telegram/SourceFiles/api/api_sending.cpp b/Telegram/SourceFiles/api/api_sending.cpp
--- a/Telegram/SourceFiles/api/api_sending.cpp
+++ b/Telegram/SourceFiles/api/api_sending.cpp
@@ -1,6 +1,12 @@
 /*
 This file is part of Telegram Desktop,
 ... (stable license header) ...
 */
+
+// TGVHOOK v0.2: include bridge shim for outgoing packet wrapping.
+#include "tgverity/tgverity_bridge_shim.h"
+
 #include "api/api_sending.h"

@@ -<approx send entry>-
 void Send(...) {
     // ... existing outgoing send path ...
+
+    // TGVHOOK BEGIN (v0.2): outgoing send wrap
+    // Intent: if the target chat is TGVerity-enabled, replace the outgoing
+    // plaintext with a TGVerity wire-text packet BEFORE the MTProto send.
+    // Keep the local random_id so updateMessageID can bind packet_id -> msg_id.
+    // Re-pin: call site is the single outgoing-text dispatch in api_sending
+    //   (around Api::SendReply / Api::SendExistingMedia shared text path).
+    if (history->session().TgVerityEnabled(history->peer)) {
+        const auto wire = tgv_shim::buildRelayText(
+            text.toStdString(),
+            history->session().tgVerityNextPacketId());
+        text = QString::fromStdString(wire);
+    }
+    // TGVHOOK END
 }
diff --git a/Telegram/SourceFiles/api/api_updates.cpp b/Telegram/SourceFiles/api/api_updates.cpp
--- a/Telegram/SourceFiles/api/api_updates.cpp
+++ b/Telegram/SourceFiles/api/api_updates.cpp
@@ -1,6 +1,12 @@
 /*
 This file is part of Telegram Desktop,
 ... (stable license header) ...
 */
+
+// TGVHOOK v0.2: include bridge shim for incoming parse + id bind.
+#include "tgverity/tgverity_bridge_shim.h"
+
 #include "api/api_updates.h"

@@ -<approx updateNewMessage handler>-
 void onSendOrUpdateMessage(...) {
     // ... existing updateNewMessage path ...
     // (item construction from MTPDupdateNewMessage)
+
+    // TGVHOOK BEGIN (v0.2): incoming packet detect + consume
+    // Intent: inspect the incoming item's text; if it is a TGVerity packet,
+    //   parse it, hand body to the adapter (which feeds core), and mark the
+    //   raw item for render/notification suppression (see history_item hook).
+    // Re-pin: the exact updateNewMessage -> HistoryItem creation call site,
+    //   after `item` is available and before notification scheduling.
+    const auto raw = item->originalText().string.toStdString();
+    if (tgv_shim::isTgVerityPacket(raw)) {
+        if (const auto parsed = tgv_shim::parse(raw)) {
+            // adapter.onIncomingText(peerId, parsed->body, parsed->packetId);
+            item->markTgVerityConsume();  // signals skipNotification + render suppress
+        }
+    }
+    // TGVHOOK END
 }

@@ -<approx updateMessageID handler>-
 void onUpdateMessageId(...) {
     // ... existing updateMessageID(random_id -> msg_id) bind ...
+
+    // TGVHOOK BEGIN (v0.2): bind send-id -> server msg_id
+    // Intent: when the server assigns the final msg_id for our outgoing random_id,
+    //   tell the adapter so core can move the packet local_pending -> tg_sent.
+    // Re-pin: the registerSentMessageRandomIds / random_id-to-msg_id resolution
+    //   site inside api_updates (symbol name varies per upstream commit).
+    // adapter.onMessageIdBound(randomId, serverMsgId);
+    // TGVHOOK END
 }
diff --git a/Telegram/SourceFiles/data/data_histories.cpp b/Telegram/SourceFiles/data/data_histories.cpp
--- a/Telegram/SourceFiles/data/data_histories.cpp
+++ b/Telegram/SourceFiles/data/data_histories.cpp
@@ -1,6 +1,10 @@
 /*
 This file is part of Telegram Desktop,
 ... (stable license header) ...
 */
+
+// TGVHOOK v0.2: include bridge shim (delete-revoke policy hook reads TGV marks).
+#include "tgverity/tgverity_bridge_shim.h"
+
 #include "data/data_histories.h"

@@ -<approx deleteMessages revoke branch>-
 void History::destroyAll(...) {
     // ... inside deleteMessages(..., bool revoke) where revoke == true ...
+
+    // TGVHOOK BEGIN (v0.2): cleanup revoke delete
+    // Intent: TGVerity raw MSG + ACK cleanup must go through ONE revoke=true
+    //   (delete-for-all) call, batched <=100 ids, never self-then-revoke.
+    //   Skip undeletable ids (MESSAGE_DELETE_FORBIDDEN) without aborting the batch.
+    // Re-pin: the revoke branch of deleteMessages; the adapter owns batching +
+    //   ordering, so this hook only confirms revoke=true is preserved for the
+    //   TGVerity-initiated cleanup path.
+    // if (tgVerityCleanupBatch) { revoke = true; /* adapter.deleteMessagesRevoke */ }
+    // TGVHOOK END
 }
diff --git a/Telegram/SourceFiles/history/history_item.cpp b/Telegram/SourceFiles/history/history_item.cpp
--- a/Telegram/SourceFiles/history/history_item.cpp
+++ b/Telegram/SourceFiles/history/history_item.cpp
@@ -1,6 +1,10 @@
 /*
 This file is part of Telegram Desktop,
 ... (stable license header) ...
 */
+
+// TGVHOOK v0.2: include bridge shim for raw-packet notification suppression.
+#include "tgverity/tgverity_bridge_shim.h"
+
 #include "history/history_item.h"

@@ -<approx skipNotification()>-
 bool HistoryItem::skipNotification() const {
     // ... existing skipNotification logic ...
+
+    // TGVHOOK BEGIN (v0.2): suppress raw notification
+    // Intent: a raw TGVerity packet must NOT produce a Telegram notification;
+    //   the adapter emits the controlled virtual-message notification instead.
+    // Re-pin: inside skipNotification() (preferred) OR inside
+    //   window/notifications_manager.cpp message-eligibility check, whichever
+    //   the current upstream commit exposes.
+    if (const auto t = originalText().string.toStdString();
+        tgv_shim::isTgVerityPacket(t)) {
+        return true;  // suppress raw packet notification
+    }
+    // TGVHOOK END
     return false;
 }
diff --git a/Telegram/SourceFiles/history/view/history_view_context_menu.cpp b/Telegram/SourceFiles/history/view/history_view_context_menu.cpp
--- a/Telegram/SourceFiles/history/view/history_view_context_menu.cpp
+++ b/Telegram/SourceFiles/history/view/history_view_context_menu.cpp
@@ -1,6 +1,10 @@
 /*
 This file is part of Telegram Desktop,
 ... (stable license header) ...
 */
+
+// TGVHOOK v0.2: include bridge shim for edit-affordance suppression.
+#include "tgverity/tgverity_bridge_shim.h"
+
 #include "history/view/history_view_context_menu.h"

@@ -<approx edit action population>-
 void FillMessageEditAction(...) {
     // ... existing code that adds the "Edit" menu action ...
+
+    // TGVHOOK BEGIN (v0.2): suppress edit menu
+    // Intent: TGVerity messages are immutable; "edit" is disabled and a
+    //   correction is sent as a new MSG. Hide the edit affordance when the
+    //   item is a TGVerity packet (raw or virtual).
+    // Re-pin: the function that populates the Edit action in the context menu
+    //   (or history_widget.cpp saveEditMessage guard). Guard before adding the
+    //   QAction, not after.
+    if (tgv_shim::isTgVerityPacket(itemText.toStdString())) {
+        return;  // do not add Edit action
+    }
+    // TGVHOOK END
 }
